[pytest]
addopts = -ra --strict-markers
testpaths = tests
markers =
    functionality: end-to-end feature and navigation checks
    load: browser page-load and interaction performance checks
    vulnerability: browser-level security regression checks
    validation: client-side form and workflow validation checks
    uiux: responsive layout and usability checks
